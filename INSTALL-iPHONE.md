# 拾光工作台 - iPhone 安装说明

## 推荐方式：HTTPS 安装

1. 将压缩包内的全部文件上传到支持 HTTPS 的静态网站服务。
2. 用 iPhone 的 Safari 打开网站地址。
3. 点击 Safari 底部的“分享”按钮。
4. 选择“添加到主屏幕”，名称保留为“拾光工作台”。
5. 点击“添加”，之后从 iPhone 主屏幕启动。

安装后应用会以独立窗口运行，并缓存核心页面，短时断网也可以打开。待办、账目、文件标签和个人记录目前保存在该设备的浏览器存储中。

在“我的”中可以修改用户名和团队成员名称，修改结果会同步到首页问候、头像和每日工作总结。每日总结、进行中任务、待办、账目和文档归档均支持本地保存。

“每日诗词”中新增了小红书意境灵感卡片，按山水治愈、江南烟雨、月色思念、人间清醒等主题跳转到小红书关键词搜索页。

打开“每日诗词”后点击“分享诗句”，iPhone 会打开系统分享面板，可选择微信联系人、朋友圈或小红书等已安装的 App。浏览器不能直接指定第三方 App 内的具体联系人；如果当前浏览器不支持系统分享，应用会自动复制诗句，之后可粘贴分享。

首页聚焦卡片会每日轮换一条短小的抽象脑筋急转弯梗。

发送填写链接使用 iPhone 的系统分享面板；自动发送到微信群/飞书/钉钉群，以及 WPS 云端同步，需要进一步配置对应平台的开放平台应用、OAuth 和服务端接口。当前界面会明确显示“待授权配置”，不会伪造已同步状态。

## 本机预览

在项目目录运行：

```bash
python3 -m http.server 4174 --directory outputs
```

然后在 Mac 浏览器打开 `http://127.0.0.1:4174/`。

## 原生 IPA 说明

可直接安装到真机的原生 `.ipa` 必须使用完整 Xcode、Apple ID/Apple Developer Team 和对应设备签名。本包是无需 App Store 审核即可添加到 iPhone 主屏幕的 PWA 版本，不包含签名 `.ipa`。
